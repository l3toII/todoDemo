framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        envelope:
            sender: '%env(MAILER_FROM_ADDRESS)%'
        headers:
            from: 'GTD Todo App <%env(MAILER_FROM_ADDRESS)%>'
